// To change a team from the command line
attr = {
	"City": "Houston",
	"TeamName": "Texans",
	"headerBGColor": "#000077",
	"headerColor": "#EE0000"
};

t = new (require('models/team'))(attr);
App.frame.model.set('team', t);

t = new (require('models/team'))(attr);
App.frame.model.set('team', t);

// Url to request data
http://localhost/backbone/index.php?module=teamData&controller=league&raw=