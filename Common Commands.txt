attr = {
	"location": "Houston",
	"name": "Texans",
	"headerBGColor": "#000077",
	"headerColor": "#EE0000"
};

t = new (require('models/team'))(attr);
App.frame.headerView.model.set('team', t);

t = new (require('models/team'))(attr);
App.frame.headerView.model.set('team', t);

